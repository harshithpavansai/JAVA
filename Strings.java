public class Strings {
    public static void main(String[] args) {
        String s1 = "hello";// string1.equals(string2);
        String s2 = "helli";
        boolean result = s1.equals(s2);
        System.out.println(result);
        // comparing 2 strings
        // use compareTo("T");
        int res = s1.compareTo(s2);
        System.out.println(res);

    }
}
